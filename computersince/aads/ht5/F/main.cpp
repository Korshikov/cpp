
#include <cstdio>
#include <cstdlib>
#include <climits>
#include <vector>
#include <set>
#include <algorithm>

using namespace std;


int main()
{
    freopen("minsum.in","r",stdin);
    //freopen("minsum.out","w+",stdout);

    unsigned n;
    scanf("%u",&n);
    int arra[n];
    int min;
    for(unsigned i=0;i<n;++i)
    

    fclose(stdin);
    fclose(stdout);

    return 0;
}
