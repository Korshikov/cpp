#include <algorithm>
#include <cstdio>
#include <vector>

using namespace std;

vector<unsigned> b1,b2;

void recover()
{
	unsigned n = b1.size();
}

int main()
{
	freopen("invers2.in","r",stdin);
    //freopen("invers2.out","w+",stdout);
    unsigned n;
	while (scanf("%u", &n) != EOF)
	{
   		b1.push_back(n);
	}


    fclose(stdin);
    fclose(stdout);
	return 0;
}