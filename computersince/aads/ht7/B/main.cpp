#include <algorithm>
#include <climits>
#include <cstdio>
#include <map>
#include <vector>

using namespace std;

int const MAX_N = 100;

std::vector<int> graph[MAX_N];

int main()
{
    freopen("connect.in","r",stdin);
    freopen("connect.out","w+",stdout);

    unsigned long n,m;
    scanf("%lu %lu",&n,&m);
    
    unsigned long b1,b2;
    while(--m)
    {
        scanf("%lu%lu",&b1,&b2);
        
    }

    


    fclose(stdin);
    fclose(stdout);

    return 0;
}
